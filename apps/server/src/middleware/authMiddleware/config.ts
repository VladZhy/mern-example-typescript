import { STATUS_CODES } from '../../config';

export const TOKEN_VERIFICATION_ERROR_MESSAGE: string =
	'Not authorized, token verification failed';
export const NO_TOKEN_ERROR_MESSAGE: string = 'Not authorized, no token';

export { STATUS_CODES };
