import styled from 'styled-components';
import StyledButtonWrapper from 'style/buttons/StyledButtonWrapper';

const StyledPreviewDeleteButton = styled(StyledButtonWrapper)`
	position: absolute;
	top: 0;
	right: 0;
`;

export default StyledPreviewDeleteButton;
