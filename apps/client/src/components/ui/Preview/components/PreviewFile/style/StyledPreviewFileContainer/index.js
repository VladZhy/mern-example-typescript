import styled from 'styled-components';
import StyledPreviewItemContainer from '../../../shared/style/StyledPreviewItemContainer';

const StyledPreviewFileContainer = styled(StyledPreviewItemContainer)`
	border-color: green;
	overflow: hidden;
`;

export default StyledPreviewFileContainer;
