import styled from 'styled-components';
import StyledActionButton from 'style/buttons/StyledActionButton';

const StyledActionIconButton = styled(StyledActionButton)`
  position: relative;
`;

export default StyledActionIconButton;
